The program uses a breadth-first search algorithm to find the shortest distance between two nodes. The nodes are comprised of exoplanets from the Exoplanets.csv file. 
The code also outputs the neighbors of a random node/exoplanet.
